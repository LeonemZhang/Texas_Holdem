# ADR-002 — 客户端只渲染服务端投影

## 背景

不同玩家需要看到不同的私有牌面和当前合法行动，桌面和浏览器还必须保持一致。

## 决策

服务端为每个玩家生成过滤后的快照；客户端不自行计算赢家、底池、牌型、下注历史或牌局阶段。

## 影响

快照投影和协议 schema 成为信息隔离边界，UI 预览也必须复用正式组件和正式投影规则。

完整行为规范：`docs/product-specs/gameplay.md#结算`
设计文档：`docs/design-docs/client.md`
