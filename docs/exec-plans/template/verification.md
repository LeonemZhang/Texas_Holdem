# 变更名称验收证据

任务转为 `done` 前，为该任务添加同名二级标题并填写所有字段；没有适用不变量时在“覆盖不变量”写明“无”。

## CHANGE-001

### 覆盖不变量

- `INV-AUTH-001`

### 自动化验证

- `pnpm --filter <workspace> test`：填写本次实际结果。
- `pnpm check`：填写本次实际结果。

### 场景

描述人工或集成场景；不适用时写明“不适用”及原因。

### 执行方式

记录环境、命令入口和必要参数，不记录密钥或个人路径。

### 覆盖状态

说明完成、部分覆盖或受阻，并解释与完成条件的对应关系。

### 证据

链接测试、日志、截图或 `docs/verification/` 中的稳定证据；不能只写“已通过”。
