# v1.0.6 最终 Windows 与联机验收记录

验收日期：2026-08-17（Asia/Shanghai）

## 验收环境

- Microsoft Windows 11 x64。
- 系统版本：`10.0.26200`。
- Node.js：`24.14.0`。
- pnpm：`10.0.0`。

本记录覆盖当前 Windows 11 单机环境、本地 Host、浏览器/Electron 测试和 Windows 产物 smoke；不把 Windows 10 独立机器或跨公网 EasyTier 节点验收结果推断为已完成。

## 质量门禁

- `pnpm install --frozen-lockfile`：通过，lockfile 无需更新。
- `pnpm version:check`：通过，源码版本一致为 `1.0.6`。
- `pnpm harness:check`：通过。
- `pnpm harness:test`：通过，26 个测试。
- `pnpm architecture:check`：通过，扫描 332 个 workspace 源码文件。
- `pnpm format:check`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过，包含 MCP Server、Client、Desktop 和 Host。
- `pnpm check:full`：通过。
- `pnpm test`：通过；poker-core 32 个文件/221 个测试，protocol 13/90，lan-discovery 5/20，MCP Server 4/48，Host 46/246，Client 41/344，Desktop 14/36。
- `pnpm build`：通过；品牌资源检查通过，ICO 包含 16、24、32、48、64、128、256 尺寸。
- `pnpm test:e2e`：通过，10 个测试文件、15 个场景。
- `pnpm audit --prod`：通过，未发现已知生产依赖漏洞。
- `pnpm package:win`：通过。

E2E04 原有筹码请求夹具已补充显式拒绝自动筹码重置投票，以继续验证拒绝重置后筹码请求和截止暂离路径；未放宽生产逻辑。

## 正式产物

| 文件                         | 大小（bytes） | SHA-256                                                            |
| ---------------------------- | ------------: | ------------------------------------------------------------------ |
| `Texas Holdem-1.0.6-x64.msi` |   115,707,904 | `E338F8B6DFBF011D19C16A89AB4B9BC6D1B8B247D4540E40DE0401B73EA48467` |
| `Texas Holdem-1.0.6-x64.zip` |   142,359,189 | `C7BF6675907A970EFC6C48E58C0A6801AC31BB580FB55ACFA42FAA899E964AE3` |
| `SHA256SUMS.txt`             |           186 | 内容列出上述两个文件                                               |

SHA-256 已使用 `Get-FileHash` 与 `SHA256SUMS.txt` 逐项复核。

ZIP 共 93 个文件，包含：

- `Texas Holdem.exe`。
- `LICENSE.txt`。
- `THIRD_PARTY_NOTICES.md`。

MSI 数据库验证结果：

- `ProductName=Texas Holdem`。
- `ProductVersion=1.0.6.0`。
- `UpgradeCode={B419EE84-D582-4FA3-9F65-60D45105F7B8}`，与历史版本一致。

## Windows 产物 smoke

### ZIP

1. ZIP 成功解压到独立临时目录。
2. `Texas Holdem.exe` 成功启动并持续运行通过 5 秒检查。
3. `LICENSE.txt` 和 `THIRD_PARTY_NOTICES.md` 均存在。
4. smoke 结束后停止本次启动的成品进程并清理临时目录。

### MSI

1. 在此前未安装 Texas Holdem 的环境中，使用带引号的绝对 MSI 路径静默安装成功，退出码为 `0`。
2. 卸载项显示版本 `1.0.6.0`，安装目录为 Windows per-user 默认目录 `C:\Users\76458\AppData\Local\Programs\Texas Holdem`。
3. 安装目录包含 `Texas Holdem.exe`。
4. 安装版成功启动并持续运行通过 5 秒检查。
5. 静默卸载成功，退出码为 `0`；卸载项和安装目录下的 `Texas Holdem.exe` 均已移除。

## 尚未完成的外部环境验收

- Windows 10 x64 的独立安装、启动和最小联机流程。
- 两个不同公网出口之间的 EasyTier 虚拟 IP 连通、最小牌局和关闭 UDP 广播中继后的 IP 直连。
- EasyTier UDP 广播中继下的跨虚拟网房间扫描。

这些项目需要额外操作系统或真实外部网络环境，不能由当前单机 Windows 11 环境替代。
