# v1.0.5 最终 Windows 与联机验收记录

验收日期：2026-08-15（Asia/Shanghai）

## 验收环境

- Microsoft Windows 11 x64。
- 系统版本：`10.0.26200`。
- Node.js：`24.14.0`。
- pnpm：`10.0.0`。

本记录覆盖当前 Windows 11 单机环境、本地 Host、浏览器/Electron 测试和 Windows 产物 smoke；不把 Windows 10 独立机器或跨公网 EasyTier 节点验收结果推断为已完成。

## 质量门禁

- `pnpm version:check`：通过，源码版本一致为 `1.0.5`。
- `pnpm harness:check`：通过。
- `pnpm harness:test`：通过，26 个测试。
- `pnpm architecture:check`：通过，扫描 332 个 workspace 源码文件。
- `pnpm format:check`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过，包含 MCP Server、Client、Desktop 和 Host。
- `pnpm check`：通过。
- `pnpm test`：通过；poker-core 32 个文件/221 个测试，protocol 13/86，lan-discovery 5/20，MCP Server 4/48，Host 46/228，Client 41/337，Desktop 14/36。
- `pnpm build`：通过；品牌资源检查通过，ICO 包含 16、24、32、48、64、128、256 尺寸。
- `pnpm test:e2e`：通过，10 个测试文件、15 个场景。
- `pnpm audit --prod`：通过，未发现已知生产依赖漏洞。
- `pnpm package:win`：通过。

## 本次发布专项验证

- Client targeted tests：10 个文件、116 个测试通过。
- Host targeted tests：6 个文件、57 个测试通过。
- MCP Server unit tests：4 个文件、48 个测试通过。
- MCP closed-loop：通过 1 个真实三手牌场景；包含 Host 与 MCP Agent 双进程重启、同身份恢复、四街推进和 SQLite 落盘检查。
- MCP 闭环测试夹具将准备窗口设为 60 秒，以覆盖自动 Runout 完整计时和重启恢复耗时；不改变产品默认或运行时规则。

## 正式产物

| 文件                         | 大小（bytes） | SHA-256                                                            |
| ---------------------------- | ------------: | ------------------------------------------------------------------ |
| `Texas Holdem-1.0.5-x64.msi` |   115,695,616 | `33A52353164D1D265CD5A43BB54A9B687143D40BA20EC34B15F7B77977FB12CA` |
| `Texas Holdem-1.0.5-x64.zip` |   142,353,125 | `16558298D2B4ED73DB56D8599AD7EE52D3047D3F3C1F18DC3AE433A7377D2557` |
| `SHA256SUMS.txt`             |           186 | 内容列出上述两个文件                                               |

SHA-256 已使用 `Get-FileHash` 与 `SHA256SUMS.txt` 逐项复核。

ZIP 共 98 个条目，可完整解压，根目录包含：

- `Texas Holdem.exe`。
- `LICENSE.txt`。
- `THIRD_PARTY_NOTICES.md`。

MSI 数据库验证结果：

- `ProductName=Texas Holdem`。
- `ProductVersion=1.0.5.0`。
- `UpgradeCode={B419EE84-D582-4FA3-9F65-60D45105F7B8}`。
- MSI 未进行 Authenticode 代码签名，与发布说明一致。

## Windows 产物 smoke

### ZIP

1. ZIP 成功解压到独立临时目录。
2. `Texas Holdem.exe` 成功启动并持续运行通过 5 秒检查。
3. `LICENSE.txt` 和 `THIRD_PARTY_NOTICES.md` 均存在。
4. smoke 结束后停止本次启动的成品进程。

### MSI

1. 在此前未安装 Texas Holdem 的环境中静默安装成功，退出码为 `0`。
2. 卸载项显示版本 `1.0.5.0`，安装目录为 Windows per-user 默认目录。
3. 安装目录包含 `Texas Holdem.exe`。
4. 安装版成功启动并持续运行通过 5 秒检查。
5. 静默卸载成功，退出码为 `0`；1.0.5 卸载项已移除。

## 尚未完成的外部环境验收

- Windows 10 x64 的独立安装、启动和最小联机流程。
- 两个不同公网出口之间的 EasyTier 虚拟 IP 连通、最小牌局和关闭 UDP 广播中继后的 IP 直连。
- EasyTier UDP 广播中继下的跨虚拟网房间扫描。

这些项目需要额外操作系统或真实外部网络环境，不能由当前单机 Windows 11 环境替代。
