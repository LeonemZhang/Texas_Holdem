# v1.0.1 最终 Windows 与联机验收记录

验收日期：2026-08-06（Asia/Shanghai）

## 验收环境

- Microsoft Windows 11 专业版 x64。
- 系统版本：`10.0.26200`。
- Node.js：`24.14.0`。
- pnpm：`10.0.0`。

当前环境没有独立 Windows 10 x64 机器，也没有位于另一个公网出口的 EasyTier 节点。因此本记录不把 Windows 10 和跨公网 EasyTier 标记为已完成实测；README 中的 Windows 10/11 是目标支持范围，EasyTier 章节是基于虚拟 IP 直连能力与官方组网方式的使用建议。

## 质量门禁

- `pnpm install --frozen-lockfile`：通过。
- `pnpm format:check`：通过。
- `pnpm lint`：通过。
- `pnpm typecheck`：通过；先构建 5 个共享包，再通过全部 8 个类型检查项目。
- `pnpm test`：通过；客户端 36 个测试文件、196 个测试，宿主 44 个测试文件、164 个测试，桌面端 13 个测试文件、33 个测试，以及共享包测试全部通过。
- `pnpm build`：通过。
- `pnpm test:e2e`：通过，共 9 个测试文件、13 个场景。
- `pnpm audit --prod`：通过，未发现已知生产依赖漏洞。
- `pnpm package:win`：通过。

### GitHub Actions

修复后的提交 [`b402103`](https://github.com/LeonemZhang/Texas_Holdem/commit/b40210377486c0c8eaa3e76bc12bef159234c547/checks) 的 `quality` 和 `windows-smoke` 均成功。

## UiSmokePreview 当前组件核验

`apps/client/src/test/UiSmokePreview.tsx` 已按当前组件契约提供 `actionOrder`、`lastAction`、结算数据和 `handPeaks`，而不是只渲染旧版界面外壳。回归测试确认牌桌出现行动顺序，并且统计面板可以打开“牌型记录”视图。

README 预览使用 Playwright 从当前预览路由重新生成，覆盖以下 5 个场景：牌局、结算、大厅与座位管理、牌局统计、筹码交换。每个场景均有：

- 桌面端：`1920×1080`。
- 手机端：`393×852`。

统计截图实际打开了当前“牌型记录”标签，并显示全局与玩家最佳五张牌数据。

## 正式产物

| 文件 | 大小（bytes） | SHA-256 |
| --- | ---: | --- |
| `Texas Holdem-1.0.1-x64.msi` | 115,662,848 | `A3DF80869871446E12CACE1F916F02D79BE634AB07A8BAD4D4C021BBA33DB800` |
| `Texas Holdem-1.0.1-x64.zip` | 142,320,866 | `1556C5C91E33754A2DFE9FD4B5D786F37F141CF32A467BDE79D2D506CC82242F` |

MSI 数据库验证结果：

- `ProductName=Texas Holdem`。
- `ProductVersion=1.0.1.0`。
- `Manufacturer=LeonemZhang`。
- `UpgradeCode={B419EE84-D582-4FA3-9F65-60D45105F7B8}`。
- 产物未进行 Authenticode 代码签名，与 README 声明一致。

ZIP 共 93 个文件条目，可完整解压，根目录包含 `Texas Holdem.exe`、`LICENSE.txt` 和 `THIRD_PARTY_NOTICES.md`。

## Windows 产物 smoke

### MSI

1. 使用正确引用的 MSI 路径静默安装成功，退出码为 `0`。
2. 卸载项显示 `Texas Holdem 1.0.1.0`。
3. 安装目录中的 `Texas Holdem.exe` 成功启动，观察到 5 个 Texas Holdem/Electron 进程。
4. 静默卸载成功，退出码为 `0`；卸载项无残留。

### ZIP

1. ZIP 成功解压并通过文件完整性检查。
2. 包含可执行文件、GPL 文本和第三方声明。

## 尚未完成的外部环境验收

- Windows 10 x64 的独立安装、启动和最小联机流程。
- 两个不同公网出口之间的 EasyTier 虚拟 IP 连通、最小牌局和关闭 UDP 广播中继后的 IP 直连。
- EasyTier UDP 广播中继下的跨虚拟网房间扫描。

这些项目需要额外操作系统或真实外部网络环境，不能由当前单机 Windows 11 环境替代。
