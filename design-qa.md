# LUNA-UI09 第二轮视觉 QA

## 审查范围

- 第一轮基准：`output/全端布局整改验证-2026-08-04/`。
- 第二轮证据：`output/全端布局二轮优化验证-2026-08-04/`。
- 组合比较：
  - `视觉对比_手机端_一轮与二轮.png`
  - `视觉对比_桌面端_一轮与二轮.png`
- 本轮重点：移动端双加注按钮、紧凑筹码 Sheet、行动者队首、本人行动摘要、牌组左对齐，以及桌面宽桌布、十人安全环带、本人底牌避让和右侧提交列。

## 实视口结果

### 手机端

- `360×640`：弃牌、过牌、跟注、全押和“调整加注 / 确认加注”均持续可达；页面无横向滚动，未发现纵排按钮文字。
- 加注 Sheet 位于操作栏上方：Sheet 底部 `536.61px`，双加注按钮顶部 `589.41px`，两者不重叠。
- 筹码触控框为 `44×44px`，可见筹码圆盘为 `33.59×33.59px`；正负筹码均为一行七枚。
- 对手行动时，真实行动者位于队首；本人行动时只有一张 `aria-hidden`、不可交互的视觉摘要位于队首，真实本人座位仍固定在桌布下沿。
- `412×732` 下公共牌和本人底牌左边界差值为 `0px`。

### 桌面端

- `1920×1040` 下桌布为 `1152×648px`，十个座位之间的相交数量为 `0`，页面无横向滚动。
- 公共牌和本人底牌左边界差值为 `0px`；两张实际底牌均不与本人座位卡相交。
- 正负筹码分别占一行；“确认加注”位于提交列上方，“清零”位于其下方；筹码触控框保持 `44×44px`。
- `1366×768` 压力视口使用纵向滚动，文档高度 `906px`；本人座位底部 `659.45px`，操作区顶部 `719.72px`，两者不重叠。

## 交互与可访问性

- “调整加注”只打开或收起 Sheet，不发送行动；关闭后保留草稿。
- “确认加注 N”直接提交当前草稿；未调整时提交最小合法加注。
- 移动座位队列启用横向惯性滚动和吸附，行动者变化时复位到队首；桌面座位坐标不随行动者变化。
- 所有关键触控目标不小于 `44px`；未发现页面横向滚动、逐字纵排或关键操作不可达。
- 浏览器控制台异常、警告事件：`0`。

## 自动化验证

- 客户端：31 个测试文件、146 个测试全部通过。
- 桌面端：10 个测试文件、27 个测试全部通过。
- E2E：9 个测试文件、13 个测试全部通过。
- 根级 `typecheck`、`lint`、`format:check`、`build` 全部通过。
- CodeGraph 索引状态：up to date。

final result: passed

---

# LUNA-UI09 第三轮与 LUNA-UI13 第二轮 QA

## 交付范围

- 桌面桌布扩至最大 `80rem`，底部行动区继续限制为 `72rem`。
- 牌局进度、倒计时、公共牌、底池纵列和本人底牌改为同一中央流式网格。
- 移动行动者使用唯一“行动中 · 顺位 N”组合标签；桌面保留完整行动提示。
- 桌面跟注金额改为单行徽标，加注滑块和三项统计共享 `17rem` 轨道。
- 移动工具面板隔离下注区，退出 `×` 在工具面板和结算期间持续显示。
- 结算公共牌、玩家底牌和最佳五张牌通过共享标签列对齐；手机结算由右上角按钮折叠。
- 根据评审补充要求，本轮未生成或交付截图。

## 实视口与几何结果

### 桌面端

- `1920×1040`：桌布为 `1280×720px`；公共牌、底池纵列和本人底牌相对桌布中心偏差均为 `0px`；十个座位相交数为 `0`，本人底牌与本人座位不相交。
- `1920×1040`：加注滑块与三项统计左右边界偏差均为 `0px`；跟注内容无换行或横向溢出。
- `1366×768`：低高度桌布收缩为 `1152×648px`，文档高度 `906px` 并允许纵向滚动；十个座位相交数为 `0`，本人座位与操作区不相交。
- `1280×800`：桌布为 `1152×648px`，文档高度 `907px`；本人座位与操作区不相交。
- `600×900` 还原窗口：加注区右边界位于操作区内部，页面无横向滚动。

### 手机端

- `360×640`：公共牌、底池纵列和本人底牌中心偏差均小于 `0.01px`；牌局状态不与倒计时或公共牌相交，本人底牌不与本人座位相交，页面无横向滚动。
- 超长牌局状态的可见宽度为 `227px`、内容宽度为 `528px`，计算样式为 `text-overflow: ellipsis`、`white-space: nowrap` 和 `overflow: hidden`；完整内容保留在 `title`，且不进入公共牌区域。
- 退出入口实测为 `44×44px`；移动行动组合标签未与座位内容相交。
- 结算公共牌、首名玩家底牌和最佳五张牌实际左边界偏差均为 `0px`；五张公共牌保持单行且无页面横向滚动。
- 结算折叠按钮为 `44×44px`，与退出入口不相交；折叠后覆盖层缩为 `125.94×44px`，三个全局工具入口仍可访问。
- 统计面板滚动容器为 `640/3844px`，筹码交换为 `640/905px`，均可滚动到最后内容；两者打开时下注区计算样式为 `display: none`，退出入口仍为 `44×44px`。

## 自动化结果

- LUNA-UI09 聚焦测试：5 个文件、45 项通过。
- LUNA-UI13 聚焦测试：3 个文件、28 项通过。
- 全量客户端：31 个文件、152 项通过。
- 全量桌面端：10 个文件、27 项通过。
- 全量房主服务：42 个文件、151 项通过。
- E2E：9 个文件、13 项通过。
- 根级 `typecheck`、`lint`、`format:check` 和 `build` 通过。
- 真实浏览器控制台：错误 `0`、警告 `0`；仅有 React 开发工具提示信息。

final result: passed
